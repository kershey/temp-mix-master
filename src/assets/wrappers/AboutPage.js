import styled from 'styled-components';

const Wrapper = styled.div`
  h3 {
    text-align: center;
    margin-top: 5rem;
  }

  p {
    line-height: 2;
    color: var(--grey-500);
    width: 80%;
    margin: 0 auto;
    margin-top: 2rem;
  }
`;

export default Wrapper;
